goog.provide('model.CommittedChain');

/**
 * Commited Chain
 * @record
 */
model.CommittedChain = function() {}

/**
 * Chain ID
 * @type {!string}
 * @export
 */
model.CommittedChain.prototype.id;

